[package]
name = "wren-core"
description = "Wren core: domain, ports and use cases. Zero I/O, zero platform dependency."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# Only for the serde backward-compatibility tests — not part of the runtime.
serde_json = { workspace = true }
